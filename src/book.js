function createTitle(title) {
  return `The ${title}`;
}

function buildMainCharacter() {
 var ghoulCharacter = {
   name:'Vassya',
  age:16,
 pronouns:'she/her'
};
var bakerCharacter = {
  name:'Hannah',
 age:30,
pronouns:'she/her'
};
}


module.exports = {
  createTitle,
  buildMainCharacter,
  // saveReview,
  // calculatePageCount,
  // writeBook,
  // editBook
}
